#include "player_mt.h"

namespace dnd_game
{
Player_mt::Player_mt(const std::string& a_name, const Number a_entryRoom, Writer& a_writer)
	: Player(a_name, a_entryRoom, a_writer)
{
	// CTOR
}
}	// dnd_game

